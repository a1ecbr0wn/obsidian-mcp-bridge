{
  "name": "obsidian-mcp-workspace",
  "private": true,
  "workspaces": [
    "obsidian-mcp-bridge",
    "mcp-shim"
  ],
  "scripts": {
    "test": "npm test --workspaces --if-present"
  },
  "engines": {
    "node": ">=18"
  }
}
